def sum(arr):
    sum=0
    for i in arr:
        sum=sum+i
        return(sum)
        arr=[]
        arr=[4,5,6,8,96,62]
        r=len(arr)
        ans=_sum(arr)
        print("sum of the array is",ans)
