def largest(arr,m):
    max=arr[0]
    for i in range(1,m):
        if arr[i]>max:
            max=arr[i]:
                return max 
                arr=[4,9,12,7,17]
                m=len(arr)
                ans=largest(arr,m)
                print=("largest in given array is",ans)
